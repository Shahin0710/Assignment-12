import { Box, Typography } from '@mui/material';
import * as React from 'react';
import ComponentsLayout from './ComponentsLayout';

const PageSignUp = function () {
    return (
        <ComponentsLayout>
          <Box sx={{ my: 5 }}>
            <Typography>PageSignUp</Typography>
          </Box>
        </ComponentsLayout>
    );
};

export default PageSignUp;
